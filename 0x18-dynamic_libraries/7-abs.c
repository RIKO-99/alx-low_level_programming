#include "main.h"
#include <stdlib.h>

/*
 * _abs - function computes absolute value
 *
 * @n: value as an integer
 *
 * Return: Always 0 Success
 *
 */
int _abs(int n)
{
	 int abs (n);
	 return (0);
}
