#include "main.h"
#include <stdio.h>

/**
 * mul - checks the product of two number
 *
 * @a: integer
 * @b: integer
 *
 * Return: Always 0
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
